<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LIMITLY" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorSecondaryContainer">@android:color/white</item>
        <item name="itemRippleColor">@android:color/white</item>
    </style>

    <style name="Theme.LIMITLY" parent="Base.Theme.LIMITLY" />
</resources>